# ⚽ FBref Premier League ETL Scraper

[![Python 3.9+](https://img.shields.io/badge/python-3.9+-blue.svg)](https://www.python.org/downloads/)
[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)

Парсер статистики Премьер-лиги с сайта [FBref.com](https://fbref.com) с сохранением в базу данных SQLite.

## 🌟 Возможности

- ✅ Парсинг всех 20 команд Премьер-лиги
- ✅ Детальная статистика игроков (голы, ассисты, минуты)
- ✅ Логи матчей (даты, расписание)
- ✅ Имитация человеческого поведения для обхода блокировок
- ✅ Автоматические повторные попытки при ошибках
- ✅ SQLite база данных для анализа
- ✅ Готовые SQL запросы для анализа данных

## 📊 Что парсится

| Данные | Статус | Описание |
|--------|--------|----------|
| Команды | ✅ | Все 20 команд Премьер-лиги |
| Игроки | ✅ | ~500 игроков с детальной статистикой |
| Матчи | ✅ | Расписание и результаты матчей |
| Статистика игроков | ✅ | Голы, ассисты, минуты, карточки |

Этот проект парсит статистику Премьер-лиги с сайта [FBref.com](https://fbref.com) и сохраняет данные в базу данных SQLite.

## Установка

### 1. Создание виртуального окружения

На macOS используйте следующие команды:

```bash
# Создать виртуальное окружение
python3 -m venv venv

# Активировать виртуальное окружение
source venv/bin/activate

# После активации в терминале появится (venv) перед строкой
```

### 2. Установка зависимостей

После активации виртуального окружения:

```bash
pip install --upgrade pip
pip install -r requirements.txt
```

## Использование

### Первый запуск

```bash
# 1. Активируйте виртуальное окружение
source venv/bin/activate

# 2. Запустите парсер
python main.py
```

### Повторный запуск (очистка базы)

Если хотите начать с чистой базы данных:
```bash
# Очистить базу данных
python clean_db.py

# Запустить парсер заново
python main.py
```

### Что делает парсер:

1. ✅ Загружает список всех команд Премьер-лиги
2. ✅ Для каждой команды скачивает:
   - Логи матчей (даты, счета, посещаемость)
   - Статистику команды (голы, владение, защита)
   - Статистику игроков (голы, ассисты, минуты)
3. ✅ Сохраняет данные в `football_data.db`

**⏱️ Время выполнения:** ~20-30 минут для всех 20 команд (из-за задержек для обхода блокировок)

## Тестирование

Быстрый тест для проверки подключения и парсинга:
```bash
python test_scrape.py
```

## Решение проблем

### Ошибка 403 Forbidden

FBref имеет строгую защиту от ботов. Проект включает продвинутую имитацию человеческого поведения:

**Встроенные механизмы защиты:**
- ✅ Случайные задержки 5-10 секунд между запросами
- ✅ Длинные паузы каждые 5 запросов (15-25 секунд)
- ✅ Ротация User-Agent из 5 современных браузеров
- ✅ Имитация Referer для естественной навигации
- ✅ Автоматические повторные попытки с экспоненциальной задержкой
- ✅ Случайное время "чтения" страниц (1-3 секунды)

**Если все равно получаете 403:**
1. **Подождите 10-15 минут** - FBref может временно блокировать IP
2. **Используйте домашний интернет** - корпоративные сети часто блокируются
3. **Попробуйте VPN** с домашним IP адресом
4. **Запускайте в режиме отладки** - установите `DEBUG_MODE = True` в `config.py`
5. **Увеличьте задержки** - измените `MIN_REQUEST_DELAY` и `MAX_REQUEST_DELAY` в `config.py`

**Настройка задержек:**
```python
# В файле config.py
MIN_REQUEST_DELAY = 10.0  # Увеличить минимальную задержку
MAX_REQUEST_DELAY = 20.0  # Увеличить максимальную задержку
```

### Проблемы с SSL на новой macOS

Если возникают ошибки SSL (как в вашем случае):
```bash
# Установите сертификаты для Python
/Applications/Python\ 3.*/Install\ Certificates.command

# Или установите certifi вручную
pip install --upgrade certifi
```

### Деактивация виртуального окружения

Когда закончите работу:
```bash
deactivate
```

## 📂 Структура проекта

```
ETLfootball/
├── main.py              # Основной ETL процесс
├── scraper.py           # Логика парсинга с имитацией человека
├── db.py                # Модели базы данных SQLAlchemy
├── config.py            # Конфигурация (задержки, режим отладки)
├── query_db.py          # Готовые запросы к БД
├── queries.sql          # SQL запросы для анализа
├── test_scrape.py       # Тестовый скрипт
├── clean_db.py          # Очистка базы данных
├── requirements.txt     # Зависимости Python
└── football_data.db     # База данных SQLite (создается автоматически)
```

## 🗄️ Структура базы данных

| Таблица | Описание |
|---------|----------|
| `teams` | Информация о командах |
| `players` | Информация об игроках |
| `matches` | Результаты матчей |
| `player_stats` | Статистика игроков |
| `squad_stats` | Статистика команд (опционально) |

## 🤝 Вклад в проект

Contributions are welcome! Пожалуйста:
1. Fork проект
2. Создайте feature branch (`git checkout -b feature/AmazingFeature`)
3. Commit изменения (`git commit -m 'Add some AmazingFeature'`)
4. Push в branch (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

## 📝 Лицензия

Распространяется под лицензией MIT. См. `LICENSE` для дополнительной информации.

## ⚠️ Disclaimer

Этот проект предназначен только для образовательных целей. Пожалуйста, соблюдайте условия использования FBref.com и не перегружайте их серверы частыми запросами.

## 🙏 Благодарности

- [FBref.com](https://fbref.com) за предоставление футбольной статистики
- Сообщество Python за отличные библиотеки
